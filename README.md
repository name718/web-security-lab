# 🔐 Security Lab - 网络安全攻击案例学习平台

一个用于学习常见网络攻击的实验平台，包含 XSS、SQL注入、CSRF 等攻击案例演示。

## ⚠️ 免责声明

本项目仅供学习和研究目的，请勿将所学知识用于非法用途！

## 🛠️ 技术栈

- **前端**: Vue 3 + Vite + Pinia + Vue Router
- **后端**: Express + Node.js
- **包管理**: pnpm (monorepo)
- **数据存储**: localStorage (前端本地存储)

## 📦 项目结构

```
security-lab/
├── packages/
│   ├── lab/          # 前端 Vue3 应用
│   │   └── src/
│   │       ├── views/
│   │       │   ├── Home.vue
│   │       │   ├── XssLab.vue
│   │       │   ├── SqlInjectionLab.vue
│   │       │   └── CsrfLab.vue
│   │       └── stores/
│   │           └── labStore.js
│   └── server/       # 后端 Express 服务
│       └── src/
│           └── index.js
├── package.json
└── pnpm-workspace.yaml
```

## 🚀 快速开始

```bash
# 安装依赖
pnpm install

# 同时启动前端和后端
pnpm dev

# 或分别启动
pnpm dev:lab     # 前端 http://localhost:3000
pnpm dev:server  # 后端 http://localhost:4000
```

## 📚 攻击案例

### 1. XSS (跨站脚本攻击)
- 存储型XSS: 评论系统注入
- 反射型XSS: 搜索功能注入
- 测试payload: `<script>alert('XSS')</script>`

### 2. SQL注入
- 登录绕过: `' OR '1'='1` 或 `' OR 1=1--`
- 模拟SQL查询展示

### 3. CSRF (跨站请求伪造)
- 转账功能演示
- Token防护机制

## 👤 测试账号

| 用户名 | 密码 | 角色 | 余额 |
|--------|------|------|------|
| admin | admin123 | admin | 10000 |
| user1 | 123456 | user | 500 |
| test | test | user | 100 |
